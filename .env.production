# Next.js Frontend Production Environment Variables (.env.production)
# These variables are loaded in production mode only (npm start)
# DO NOT add secrets here - use environment variables on the server instead

# Production settings
NODE_ENV=production

# Disable debug features in production
DEBUG_TOKEN_ESTIMATE=false

# Production CORS settings (if needed for client-side code)
CORS_ALLOWED_ORIGINS=https://purpose-gemini-proto.vercel.app

# Note: For production deployment, all sensitive values should be 
# set as environment variables on your hosting platform
# (Vercel, AWS, etc.) rather than being included in this file.