# Next.js Frontend Development Environment Variables (.env.development)
# These variables are loaded in development mode only (npm run dev)

# Enable development features
NODE_ENV=development

# Enable more verbose logging
DEBUG_TOKEN_ESTIMATE=true

# Local dev origins
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://localhost:8082,http://localhost:8083,http://localhost:3000

# Note: Base variables are loaded from .env
# Sensitive variables should be in .env.local (not committed to version control)